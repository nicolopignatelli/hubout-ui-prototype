/** @jsx React.DOM */
var MultiTrack = MultiTrack || {};
MultiTrack.VerticalZoomControl = React.createClass({
  render: function() {
    return (
      <div id="multi-track_vertical-zoom-control">
      </div>
    );
  }
});
