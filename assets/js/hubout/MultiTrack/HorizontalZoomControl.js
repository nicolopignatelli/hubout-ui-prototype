/** @jsx React.DOM */
var MultiTrack = MultiTrack || {};
MultiTrack.HorizontalZoomControl = React.createClass({
  render: function() {
    return (
      <div id="multi-track_horizontal-zoom-control">
      </div>
    );
  }
});
